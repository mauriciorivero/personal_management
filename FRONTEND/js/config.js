// Configuracion global del frontend: URL base del backend (API REST).
// Al estar en un archivo separado, cambiar de entorno (local/produccion)
// solo requiere editar esta constante.
const API_BASE_URL = 'http://localhost:3000/api';
